# Runtime dependencies for the IBTool QGIS plugin.
#
# numpy is bundled with QGIS 3.40+ and does not need to be installed separately.
# scipy and networkx may or may not be bundled depending on the QGIS installation
# and platform. If the plugin fails to load, install them manually:
#
#   pip install scipy networkx
#
# For development and testing outside QGIS, see requirements-dev.txt.

numpy>=1.20
scipy>=1.7
networkx>=2.6