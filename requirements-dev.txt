# Development and testing dependencies.
# Install with: pip install -r requirements-dev.txt
#
# These packages are NOT bundled with QGIS and must be installed separately
# when running tests outside of the QGIS environment (e.g. locally or in CI).

pytest>=7.0
pytest-cov>=4.0
