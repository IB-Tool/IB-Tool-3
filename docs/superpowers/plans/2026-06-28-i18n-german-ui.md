# i18n: German UI Translation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a complete German translation for the IBTool QGIS plugin using Qt Linguist, with English as the automatic fallback for all non-German locales.

**Architecture:** English source strings remain in code, wrapped in `QCoreApplication.translate()` / `self.tr()`. A single `i18n/IBTool_de.ts` file holds all German translations across four Qt contexts (`IBToolDialogBase`, `IBTool`, `IBToolDialog`, `InputValidator`). QGIS reads the user locale from `QSettings` and auto-loads the matching `.qm` file — no English `.ts` file is needed.

**Tech Stack:** PyQt5 / Qt Linguist, `QCoreApplication.translate()`, Qt `.ts` / `.qm` format, `lrelease` (bundled with QGIS at `C:\Program Files\QGIS 3.x\bin\lrelease.exe`)

---

## File Map

| File | Action | Responsible for |
|------|--------|-----------------|
| `i18n/IBTool_de.ts` | **Create** | All German translations (4 contexts) |
| `i18n/IBTool_de.qm` | Compiled by user after plan | Binary loaded at runtime |
| `i18n/de.ts` | **Delete** | Superseded |
| `i18n/de.qm` | **Delete** | Superseded |
| `ibtool/ibtool/ibtool.py` | **Modify** | Fix path bug; wrap ~30 user-facing strings |
| `ibtool/ibtool/ibtool_dialog.py` | **Modify** | Lazy step labels; wrap ~12 strings |
| `ibtool/helpers/check.py` | **Modify** | Add `_tr()`; wrap ~40 validation messages |
| `docs/contributing.md` | **Modify** | Add "Updating translations" section |

---

## Task 1 — Create `i18n/IBTool_de.ts`

**Files:**
- Create: `i18n/IBTool_de.ts`

- [ ] **Step 1: Write the complete translation file**

Create `i18n/IBTool_de.ts` with this exact content:

```xml
<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE TS>
<TS version="2.1" language="de_DE" sourcelanguage="en">

<!-- ================================================================
     Context: IBToolDialogBase
     Source:  ibtool/ibtool/ibtool_dialog_base.ui
     Qt auto-translates <string> elements via uic.loadUiType()
     ================================================================ -->
<context>
    <name>IBToolDialogBase</name>
    <message>
        <source>IB-Tool 3</source>
        <translation>IB-Tool 3</translation>
    </message>
    <message>
        <source>① Input</source>
        <translation>① Eingabe</translation>
    </message>
    <message>
        <source>② Parameters</source>
        <translation>② Parameter</translation>
    </message>
    <message>
        <source>③ Validation</source>
        <translation>③ Validierung</translation>
    </message>
    <message>
        <source>④ Processing</source>
        <translation>④ Verarbeitung</translation>
    </message>
    <message>
        <source>──</source>
        <translation>──</translation>
    </message>
    <message>
        <source>Input Data</source>
        <translation>Eingabedaten</translation>
    </message>
    <message>
        <source>Building Footprints *</source>
        <translation>Gebäudegrundrisse *</translation>
    </message>
    <message>
        <source>Road Network *</source>
        <translation>Straßennetz *</translation>
    </message>
    <message>
        <source>Partitions *</source>
        <translation>Partitionen *</translation>
    </message>
    <message>
        <source>Auxiliary Data</source>
        <translation>Hilfsdaten</translation>
    </message>
    <message>
        <source>Output File *</source>
        <translation>Ausgabedatei *</translation>
    </message>
    <message>
        <source>Workspace *</source>
        <translation>Arbeitsverzeichnis *</translation>
    </message>
    <message>
        <source>Filter TXT (optional)</source>
        <translation>Filter TXT (optional)</translation>
    </message>
    <message>
        <source>Log Directory</source>
        <translation>Log-Verzeichnis</translation>
    </message>
    <message>
        <source>Spatial Reference</source>
        <translation>Koordinatenreferenzsystem</translation>
    </message>
    <message>
        <source>Show Filter Entries...</source>
        <translation>Filtereinträge anzeigen...</translation>
    </message>
    <message>
        <source>Settlement Analysis</source>
        <translation>Siedlungsanalyse</translation>
    </message>
    <message>
        <source>Min. Block Overlap (%)</source>
        <translation>Min. Blocküberlappung (%)</translation>
    </message>
    <message>
        <source>Minimum percentage overlap of inner blocks (0–100). Recommended: 15–25</source>
        <translation>Mindestüberlappung der inneren Blöcke (0–100). Empfehlung: 15–25</translation>
    </message>
    <message>
        <source>Global Footprint Density (%)</source>
        <translation>Globale Grundrissdichte (%)</translation>
    </message>
    <message>
        <source>Global building footprint density in %. Value 0 = calculate automatically.</source>
        <translation>Globale Grundrissdichte in %. Wert 0 = automatisch berechnen.</translation>
    </message>
    <message>
        <source>Min. Building Area (sqm)</source>
        <translation>Min. Gebäudefläche (m²)</translation>
    </message>
    <message>
        <source>Minimum building footprint area in sqm. Smaller buildings are ignored. Recommended: 50–100 sqm</source>
        <translation>Mindestfläche der Gebäudegrundrisse in m². Kleinere Gebäude werden ignoriert. Empfehlung: 50–100 m²</translation>
    </message>
    <message>
        <source>Min. Building Count</source>
        <translation>Min. Gebäudeanzahl</translation>
    </message>
    <message>
        <source>Minimum number of buildings to form a settlement area. Recommended: 10–30</source>
        <translation>Mindestanzahl Gebäude für eine Siedlungsfläche. Empfehlung: 10–30</translation>
    </message>
    <message>
        <source>Min. Patch Size (sqm)</source>
        <translation>Min. Patchgröße (m²)</translation>
    </message>
    <message>
        <source>Minimum size of an inner area patch in sqm. Recommended: 5000–20000 sqm</source>
        <translation>Mindestgröße einer Innenfläche in m². Empfehlung: 5000–20000 m²</translation>
    </message>
    <message>
        <source>Max. Hole Size (sqm)</source>
        <translation>Max. Lochgröße (m²)</translation>
    </message>
    <message>
        <source>Maximum size of holes in sqm that are automatically closed. Recommended: 5000–20000 sqm</source>
        <translation>Maximale Lochgröße in m², die automatisch geschlossen wird. Empfehlung: 5000–20000 m²</translation>
    </message>
    <message>
        <source>Max. Gap Size (sqm)</source>
        <translation>Max. Lückengröße (m²)</translation>
    </message>
    <message>
        <source>Maximum size of gaps in sqm that are closed. Recommended: 2000–10000 sqm</source>
        <translation>Maximale Lückengröße in m², die geschlossen wird. Empfehlung: 2000–10000 m²</translation>
    </message>
    <message>
        <source>Validation Checklist</source>
        <translation>Validierungsliste</translation>
    </message>
    <message>
        <source>(no validation run yet)</source>
        <translation>(noch keine Validierung durchgeführt)</translation>
    </message>
    <message>
        <source>Debugging</source>
        <translation>Fehlersuche</translation>
    </message>
    <message>
        <source>Partition Start (default -1)</source>
        <translation>Partitionsstart (Standard -1)</translation>
    </message>
    <message>
        <source>Partition End (default -1)</source>
        <translation>Partitionsende (Standard -1)</translation>
    </message>
    <message>
        <source>Partition List (default #)</source>
        <translation>Partitionsliste (Standard #)</translation>
    </message>
    <message>
        <source>Log Level</source>
        <translation>Logbuch-Stufe</translation>
    </message>
    <message>
        <source>Delete PartLog at Start</source>
        <translation>PartLog beim Start löschen</translation>
    </message>
    <message>
        <source>Save Faulty Features (Debug Mode)</source>
        <translation>Fehlerhafte Objekte speichern (Debug-Modus)</translation>
    </message>
    <message>
        <source>Ready</source>
        <translation>Bereit</translation>
    </message>
    <message>
        <source>Copy Log</source>
        <translation>Log kopieren</translation>
    </message>
    <message>
        <source>Load Result</source>
        <translation>Ergebnis laden</translation>
    </message>
    <message>
        <source>Open Directory</source>
        <translation>Verzeichnis öffnen</translation>
    </message>
    <message>
        <source>Export Log</source>
        <translation>Log exportieren</translation>
    </message>
    <message>
        <source>Save Config</source>
        <translation>Konfiguration speichern</translation>
    </message>
    <message>
        <source>Reset Config</source>
        <translation>Konfiguration zurücksetzen</translation>
    </message>
    <message>
        <source>Delete CONFIG.ini and reset all fields to defaults</source>
        <translation>CONFIG.ini löschen und alle Felder zurücksetzen</translation>
    </message>
    <message>
        <source>← Back</source>
        <translation>← Zurück</translation>
    </message>
    <message>
        <source>Next →</source>
        <translation>Weiter →</translation>
    </message>
    <message>
        <source>Check</source>
        <translation>Prüfen</translation>
    </message>
    <message>
        <source>Start</source>
        <translation>Start</translation>
    </message>
    <message>
        <source>Cancel</source>
        <translation>Abbrechen</translation>
    </message>
</context>

<!-- ================================================================
     Context: IBTool
     Source:  ibtool/ibtool/ibtool.py  (self.tr() calls)
     ================================================================ -->
<context>
    <name>IBTool</name>
    <message>
        <source>&amp;IB-Tool</source>
        <translation>&amp;IB-Tool</translation>
    </message>
    <message>
        <source>IB-Tool</source>
        <translation>IB-Tool</translation>
    </message>
    <message>
        <source>Select building footprints file</source>
        <translation>Gebäudegrundrisse auswählen</translation>
    </message>
    <message>
        <source>Select road network file</source>
        <translation>Straßennetzlayer auswählen</translation>
    </message>
    <message>
        <source>Select partitions file</source>
        <translation>Partitionslayer auswählen</translation>
    </message>
    <message>
        <source>Select auxiliary data file</source>
        <translation>Hilfsdatenlayer auswählen</translation>
    </message>
    <message>
        <source>Select output file</source>
        <translation>Ausgabedatei auswählen</translation>
    </message>
    <message>
        <source>Select workspace folder</source>
        <translation>Arbeitsverzeichnis auswählen</translation>
    </message>
    <message>
        <source>Select filter config file</source>
        <translation>Filterkonfigurationsdatei auswählen</translation>
    </message>
    <message>
        <source>Select log directory</source>
        <translation>Log-Verzeichnis auswählen</translation>
    </message>
    <message>
        <source>Vector files (*.shp *.gpkg);;Shapefiles (*.shp);;GeoPackage (*.gpkg);;All Files (*)</source>
        <translation>Vektordateien (*.shp *.gpkg);;Shapefiles (*.shp);;GeoPackage (*.gpkg);;Alle Dateien (*)</translation>
    </message>
    <message>
        <source>GeoPackage (*.gpkg);;All Files (*)</source>
        <translation>GeoPackage (*.gpkg);;Alle Dateien (*)</translation>
    </message>
    <message>
        <source>Text files (*.txt);;All Files (*)</source>
        <translation>Textdateien (*.txt);;Alle Dateien (*)</translation>
    </message>
    <message>
        <source>Starting processing...</source>
        <translation>Verarbeitung wird gestartet...</translation>
    </message>
    <message>
        <source>Processing complete</source>
        <translation>Verarbeitung abgeschlossen</translation>
    </message>
    <message>
        <source>Reset Configuration</source>
        <translation>Konfiguration zurücksetzen</translation>
    </message>
    <message>
        <source>Delete CONFIG.ini and reset all fields to defaults?
This action cannot be undone.</source>
        <translation>CONFIG.ini löschen und alle Felder zurücksetzen?
Diese Aktion kann nicht rückgängig gemacht werden.</translation>
    </message>
    <message>
        <source>Configuration loaded from CONFIG.ini.</source>
        <translation>Konfiguration aus CONFIG.ini geladen.</translation>
    </message>
    <message>
        <source>Configuration saved to CONFIG.ini.</source>
        <translation>Konfiguration in CONFIG.ini gespeichert.</translation>
    </message>
    <message>
        <source>Configuration reset — CONFIG.ini deleted.</source>
        <translation>Konfiguration zurückgesetzt — CONFIG.ini gelöscht.</translation>
    </message>
    <message>
        <source>Processing aborted due to validation errors.</source>
        <translation>Verarbeitung wegen Validierungsfehlern abgebrochen.</translation>
    </message>
    <message>
        <source>Processing cannot be cancelled mid-run.</source>
        <translation>Verarbeitung kann während des Laufs nicht abgebrochen werden.</translation>
    </message>
    <message>
        <source>Output file not found.</source>
        <translation>Ausgabedatei nicht gefunden.</translation>
    </message>
    <message>
        <source>Output directory not found.</source>
        <translation>Ausgabeverzeichnis nicht gefunden.</translation>
    </message>
    <message>
        <source>No log content to export.</source>
        <translation>Kein Log-Inhalt zum Exportieren.</translation>
    </message>
    <message>
        <source>Export log</source>
        <translation>Log exportieren</translation>
    </message>
    <message>
        <source>IB-Tool result</source>
        <translation>IB-Tool Ergebnis</translation>
    </message>
    <message>
        <source>Load Data</source>
        <translation>Daten laden</translation>
    </message>
    <message>
        <source>Calculate Blocks</source>
        <translation>Blöcke berechnen</translation>
    </message>
    <message>
        <source>Apply Filter</source>
        <translation>Filter anwenden</translation>
    </message>
    <message>
        <source>Calculate MST</source>
        <translation>MST berechnen</translation>
    </message>
    <message>
        <source>Clustering</source>
        <translation>Clustering</translation>
    </message>
    <message>
        <source>Erode Empty Areas</source>
        <translation>Leerflächen erodieren</translation>
    </message>
    <message>
        <source>Save Output</source>
        <translation>Ausgabe speichern</translation>
    </message>
</context>

<!-- ================================================================
     Context: IBToolDialog
     Source:  ibtool/ibtool/ibtool_dialog.py
     ================================================================ -->
<context>
    <name>IBToolDialog</name>
    <message>
        <source>① Input</source>
        <translation>① Eingabe</translation>
    </message>
    <message>
        <source>② Parameters</source>
        <translation>② Parameter</translation>
    </message>
    <message>
        <source>③ Validation</source>
        <translation>③ Validierung</translation>
    </message>
    <message>
        <source>④ Processing</source>
        <translation>④ Verarbeitung</translation>
    </message>
    <message>
        <source>Input</source>
        <translation>Eingabe</translation>
    </message>
    <message>
        <source>Parameters</source>
        <translation>Parameter</translation>
    </message>
    <message>
        <source>Validation</source>
        <translation>Validierung</translation>
    </message>
    <message>
        <source>Processing</source>
        <translation>Verarbeitung</translation>
    </message>
    <message>
        <source>✅  All checks passed</source>
        <translation>✅  Alle Prüfungen bestanden</translation>
    </message>
    <message>
        <source>Filter entries</source>
        <translation>Filtereinträge</translation>
    </message>
    <message>
        <source>Positive filter</source>
        <translation>Positiver Filter</translation>
    </message>
    <message>
        <source>Negative filter</source>
        <translation>Negativer Filter</translation>
    </message>
</context>

<!-- ================================================================
     Context: InputValidator
     Source:  ibtool/helpers/check.py  (_tr() calls)
     ================================================================ -->
<context>
    <name>InputValidator</name>
    <!-- Layer display names (used in error message templates) -->
    <message>
        <source>Building footprints (HU)</source>
        <translation>Gebäudegrundrisse (HU)</translation>
    </message>
    <message>
        <source>Road network (RN)</source>
        <translation>Straßennetz (RN)</translation>
    </message>
    <message>
        <source>Partitioning (Part)</source>
        <translation>Partitionen (Part)</translation>
    </message>
    <message>
        <source>Auxiliary layer (Aux)</source>
        <translation>Hilfslayer (Aux)</translation>
    </message>
    <!-- quick_path_check -->
    <message>
        <source>No path specified</source>
        <translation>Kein Pfad angegeben</translation>
    </message>
    <message>
        <source>File or directory does not exist</source>
        <translation>Datei oder Verzeichnis existiert nicht</translation>
    </message>
    <message>
        <source>Not a directory</source>
        <translation>Kein Verzeichnis</translation>
    </message>
    <!-- validate_all layer loop -->
    <message>
        <source>{layer}: No file path specified.</source>
        <translation>{layer}: Kein Dateipfad angegeben.</translation>
    </message>
    <message>
        <source>{layer}: File does not exist: {path}</source>
        <translation>{layer}: Datei existiert nicht: {path}</translation>
    </message>
    <message>
        <source>{layer}: File cannot be loaded as a valid layer: {path}</source>
        <translation>{layer}: Datei kann nicht als gültiger Layer geladen werden: {path}</translation>
    </message>
    <!-- _check_crs -->
    <message>
        <source>{layer}: CRS mismatch. Expected: {expected}, found: {actual}. Hint: Reproject the layer to {expected}.</source>
        <translation>{layer}: KRS-Konflikt. Erwartet: {expected}, gefunden: {actual}. Hinweis: Layer auf {expected} umprojizieren.</translation>
    </message>
    <!-- _check_feature_counts -->
    <message>
        <source>{layer}: Layer is empty (0 features). Hint: Populate the layer with data.</source>
        <translation>{layer}: Layer ist leer (0 Objekte). Hinweis: Layer mit Daten befüllen.</translation>
    </message>
    <message>
        <source>{layer}: Too few features ({count}), at least {min_count} required. Hint: Check the dataset for completeness.</source>
        <translation>{layer}: Zu wenige Objekte ({count}), mindestens {min_count} erforderlich. Hinweis: Datensatz auf Vollständigkeit prüfen.</translation>
    </message>
    <!-- _check_geometries -->
    <message>
        <source>{layer}: {null_count} features with NULL geometry. Hint: Remove features without geometry.</source>
        <translation>{layer}: {null_count} Objekte mit NULL-Geometrie. Hinweis: Objekte ohne Geometrie entfernen.</translation>
    </message>
    <message>
        <source>{layer}: {empty_count} features with empty geometry. Hint: Remove features with empty geometry.</source>
        <translation>{layer}: {empty_count} Objekte mit leerer Geometrie. Hinweis: Objekte mit leerer Geometrie entfernen.</translation>
    </message>
    <message>
        <source>{layer}: {invalid_count} invalid geometries (isGeosValid=False).{details} Hint: Fix geometries (native:fixgeometries).</source>
        <translation>{layer}: {invalid_count} ungültige Geometrien (isGeosValid=False).{details} Hinweis: Geometrien reparieren (native:fixgeometries).</translation>
    </message>
    <!-- _check_validity_processing -->
    <message>
        <source>{layer}: &apos;qgis:checkvalidity&apos; found {invalid_count} invalid features.{details} Hint: Fix geometries (native:fixgeometries).</source>
        <translation>{layer}: &apos;qgis:checkvalidity&apos; hat {invalid_count} ungültige Objekte gefunden.{details} Hinweis: Geometrien reparieren (native:fixgeometries).</translation>
    </message>
    <message>
        <source>{layer}: &apos;qgis:checkvalidity&apos; could not be executed: {error}</source>
        <translation>{layer}: &apos;qgis:checkvalidity&apos; konnte nicht ausgeführt werden: {error}</translation>
    </message>
    <!-- _check_hu_layer -->
    <message>
        <source>Building footprints (HU): Polygon geometry required, but {geom_type} found. Hint: Use a layer with polygon geometry.</source>
        <translation>Gebäudegrundrisse (HU): Polygongeometrie erforderlich, aber {geom_type} gefunden. Hinweis: Layer mit Polygongeometrie verwenden.</translation>
    </message>
    <message>
        <source>Building footprints (HU): Field &apos;fkt&apos;, &apos;gfkzshh&apos; or &apos;funktion&apos; missing. Available fields: {fields}. Hint: Add a field &apos;fkt&apos;, &apos;gfkzshh&apos; or &apos;funktion&apos; containing building function codes.</source>
        <translation>Gebäudegrundrisse (HU): Feld &apos;fkt&apos;, &apos;gfkzshh&apos; oder &apos;funktion&apos; fehlt. Verfügbare Felder: {fields}. Hinweis: Feld &apos;fkt&apos;, &apos;gfkzshh&apos; oder &apos;funktion&apos; mit Gebäudefunktionscodes hinzufügen.</translation>
    </message>
    <message>
        <source>Building footprints (HU): {null_count} features with empty/NULL value in field &apos;{field}&apos;. Hint: All features require a building function code.</source>
        <translation>Gebäudegrundrisse (HU): {null_count} Objekte mit leerem/NULL-Wert in Feld &apos;{field}&apos;. Hinweis: Alle Objekte benötigen einen Gebäudefunktionscode.</translation>
    </message>
    <message>
        <source>Building footprints (HU): Values in field &apos;{field}&apos; do not match the ATKIS format (NNNNN_NNNN, e.g. 31001_1000). Examples: {examples}. Hint: Only the first 10 characters are used for filter matching.</source>
        <translation>Gebäudegrundrisse (HU): Werte in Feld &apos;{field}&apos; entsprechen nicht dem ATKIS-Format (NNNNN_NNNN, z.B. 31001_1000). Beispiele: {examples}. Hinweis: Nur die ersten 10 Zeichen werden für die Filterung verwendet.</translation>
    </message>
    <!-- _check_rn_layer -->
    <message>
        <source>Road network (RN): Line geometry required, but {geom_type} found. Hint: Use a layer with line geometry.</source>
        <translation>Straßennetz (RN): Liniengeometrie erforderlich, aber {geom_type} gefunden. Hinweis: Layer mit Liniengeometrie verwenden.</translation>
    </message>
    <!-- _check_aux_layer -->
    <message>
        <source>Auxiliary layer (Aux): Line geometry required, but {geom_type} found. Hint: Use a layer with line geometry.</source>
        <translation>Hilfslayer (Aux): Liniengeometrie erforderlich, aber {geom_type} gefunden. Hinweis: Layer mit Liniengeometrie verwenden.</translation>
    </message>
    <!-- _check_part_layer -->
    <message>
        <source>Partitioning (Part): Polygon geometry required, but {geom_type} found. Hint: Use a layer with polygon geometry.</source>
        <translation>Partitionen (Part): Polygongeometrie erforderlich, aber {geom_type} gefunden. Hinweis: Layer mit Polygongeometrie verwenden.</translation>
    </message>
    <message>
        <source>Partitioning (Part): Field &apos;{field}&apos; missing. Available fields: {fields}. Hint: Add a text field &apos;NAME&apos; with partition names (e.g. PART_123).</source>
        <translation>Partitionen (Part): Feld &apos;{field}&apos; fehlt. Verfügbare Felder: {fields}. Hinweis: Textfeld &apos;NAME&apos; mit Partitionsnamen hinzufügen (z.B. PART_123).</translation>
    </message>
    <message>
        <source>Partitioning (Part): {null_count} features with empty/NULL value in field &apos;NAME&apos;. Hint: All partitions require a name in the format PART_&lt;number&gt;.</source>
        <translation>Partitionen (Part): {null_count} Objekte mit leerem/NULL-Wert in Feld &apos;NAME&apos;. Hinweis: Alle Partitionen benötigen einen Namen im Format PART_&lt;Nummer&gt;.</translation>
    </message>
    <message>
        <source>Partitioning (Part): NAME values do not match the format PART_&lt;number&gt;. Examples: {examples}. Hint: NAME values must exactly follow the pattern PART_123 (e.g. PART_36, PART_433).</source>
        <translation>Partitionen (Part): NAME-Werte entsprechen nicht dem Format PART_&lt;Nummer&gt;. Beispiele: {examples}. Hinweis: NAME-Werte müssen exakt dem Muster PART_123 folgen (z.B. PART_36, PART_433).</translation>
    </message>
    <!-- _check_multipart_lines -->
    <message>
        <source>{layer}: {count} features contain multiple line strings (MultiLineString with &gt;1 part). Each feature may only contain one line string. Hint: Explode multipart features (native:multiparttosingleparts).</source>
        <translation>{layer}: {count} Objekte enthalten mehrere Linienstrings (MultiLineString mit &gt;1 Teil). Jedes Objekt darf nur einen Linienstring enthalten. Hinweis: Mehrteilige Objekte trennen (native:multiparttosingleparts).</translation>
    </message>
    <!-- _check_part_hu_ratio -->
    <message>
        <source>Part:HU ratio = 1:{ratio} (threshold: 1:{threshold}). Part has {part_count} features, HU has {hu_count} features. Hint: Use a finer partitioning to reduce processing time per partition.</source>
        <translation>Part:HU-Verhältnis = 1:{ratio} (Schwellenwert: 1:{threshold}). Part hat {part_count} Objekte, HU hat {hu_count} Objekte. Hinweis: Feinere Partitionierung verwenden, um die Verarbeitungszeit pro Partition zu reduzieren.</translation>
    </message>
    <!-- _read_filter_file -->
    <message>
        <source>Filter file: No file path specified.</source>
        <translation>Filterdatei: Kein Dateipfad angegeben.</translation>
    </message>
    <message>
        <source>Filter file: File does not exist: {path}</source>
        <translation>Filterdatei: Datei existiert nicht: {path}</translation>
    </message>
    <message>
        <source>Filter file: File cannot be read: {error}. Hint: File must be UTF-8 encoded and readable.</source>
        <translation>Filterdatei: Datei kann nicht gelesen werden: {error}. Hinweis: Datei muss UTF-8-kodiert und lesbar sein.</translation>
    </message>
    <!-- _validate_filter_structure -->
    <message>
        <source>Filter file: Section &apos;{section}&apos; missing. Hint: Add the line &apos;{section}&apos; to the file.</source>
        <translation>Filterdatei: Abschnitt &apos;{section}&apos; fehlt. Hinweis: Zeile &apos;{section}&apos; zur Datei hinzufügen.</translation>
    </message>
    <message>
        <source>Filter file: &apos;{pos}&apos; (line {pos_line}) must appear before &apos;{neg}&apos; (line {neg_line}).</source>
        <translation>Filterdatei: &apos;{pos}&apos; (Zeile {pos_line}) muss vor &apos;{neg}&apos; (Zeile {neg_line}) erscheinen.</translation>
    </message>
    <message>
        <source>Filter file: {count} lines appear before the first section header and will be ignored. Examples: {examples}.</source>
        <translation>Filterdatei: {count} Zeilen erscheinen vor der ersten Abschnittsüberschrift und werden ignoriert. Beispiele: {examples}.</translation>
    </message>
    <!-- _validate_filter_entries -->
    <message>
        <source>Filter file: Section &apos;{section}&apos; contains no entries. Hint: Add at least one function code.</source>
        <translation>Filterdatei: Abschnitt &apos;{section}&apos; enthält keine Einträge. Hinweis: Mindestens einen Funktionscode hinzufügen.</translation>
    </message>
    <message>
        <source>Filter file: Entries do not match the ATKIS format (NNNNN_NNNN). Examples: {examples}. Hint: Only the first 10 characters are used for filter matching.</source>
        <translation>Filterdatei: Einträge entsprechen nicht dem ATKIS-Format (NNNNN_NNNN). Beispiele: {examples}. Hinweis: Nur die ersten 10 Zeichen werden für die Filterung verwendet.</translation>
    </message>
    <!-- _check_output_paths -->
    <message>
        <source>Output file: No path specified.</source>
        <translation>Ausgabedatei: Kein Pfad angegeben.</translation>
    </message>
    <message>
        <source>Output file: GeoPackage (.gpkg) required, but got: {path}. Hint: Choose a .gpkg output file.</source>
        <translation>Ausgabedatei: GeoPackage (.gpkg) erforderlich, aber erhalten: {path}. Hinweis: .gpkg-Ausgabedatei wählen.</translation>
    </message>
    <message>
        <source>Output file: Path must include a directory: {path}. Hint: Choose a full output path such as C:/data/result.gpkg.</source>
        <translation>Ausgabedatei: Pfad muss ein Verzeichnis enthalten: {path}. Hinweis: Vollständigen Ausgabepfad wählen, z.B. C:/daten/ergebnis.gpkg.</translation>
    </message>
    <message>
        <source>Workspace directory: No path specified.</source>
        <translation>Arbeitsverzeichnis: Kein Pfad angegeben.</translation>
    </message>
    <!-- _check_numeric_params — param labels -->
    <message>
        <source>Min. Overlap Blocks (%)</source>
        <translation>Min. Blocküberlappung (%)</translation>
    </message>
    <message>
        <source>Global Footprint Density (%)</source>
        <translation>Globale Grundrissdichte (%)</translation>
    </message>
    <message>
        <source>Min. Building Area (sqm)</source>
        <translation>Min. Gebäudefläche (m²)</translation>
    </message>
    <message>
        <source>Min. Building Count</source>
        <translation>Min. Gebäudeanzahl</translation>
    </message>
    <message>
        <source>Min. Patch Size (sqm)</source>
        <translation>Min. Patchgröße (m²)</translation>
    </message>
    <message>
        <source>Max. Hole Size (sqm)</source>
        <translation>Max. Lochgröße (m²)</translation>
    </message>
    <message>
        <source>Max. Gap Size (sqm)</source>
        <translation>Max. Lückengröße (m²)</translation>
    </message>
    <!-- _check_numeric_params — error templates -->
    <message>
        <source>Parameter &apos;{label}&apos;: No value specified.</source>
        <translation>Parameter &apos;{label}&apos;: Kein Wert angegeben.</translation>
    </message>
    <message>
        <source>integer</source>
        <translation>Ganzzahl</translation>
    </message>
    <message>
        <source>number</source>
        <translation>Zahl</translation>
    </message>
    <message>
        <source>Parameter &apos;{label}&apos;: &apos;{value}&apos; is not a valid {kind}.</source>
        <translation>Parameter &apos;{label}&apos;: &apos;{value}&apos; ist keine gültige {kind}.</translation>
    </message>
    <message>
        <source>Parameter &apos;{label}&apos;: Value {value} is less than the minimum ({min_val}).</source>
        <translation>Parameter &apos;{label}&apos;: Wert {value} ist kleiner als das Minimum ({min_val}).</translation>
    </message>
    <message>
        <source>Parameter &apos;{label}&apos;: Value {value} is greater than the maximum ({max_val}).</source>
        <translation>Parameter &apos;{label}&apos;: Wert {value} ist größer als das Maximum ({max_val}).</translation>
    </message>
    <!-- _check_spatial_reference_param -->
    <message>
        <source>Parameter &apos;CRS&apos;: &apos;{crs}&apos; is not a valid CRS. Hint: Use e.g. EPSG:25832.</source>
        <translation>Parameter &apos;KRS&apos;: &apos;{crs}&apos; ist kein gültiges KRS. Hinweis: Verwenden Sie z.B. EPSG:25832.</translation>
    </message>
    <message>
        <source>Parameter &apos;CRS&apos;: &apos;{crs}&apos; is a geographic CRS (degrees). Hint: A projected CRS (metres) such as EPSG:25832 is recommended.</source>
        <translation>Parameter &apos;KRS&apos;: &apos;{crs}&apos; ist ein geographisches KRS (Grad). Hinweis: Ein projiziertes KRS (Meter) wie EPSG:25832 wird empfohlen.</translation>
    </message>
    <!-- _check_partition_range_params -->
    <message>
        <source>Parameter &apos;Partition Start&apos;: Value {value} is invalid. Hint: -1 (all) or &gt;= 0.</source>
        <translation>Parameter &apos;Partitionsstart&apos;: Wert {value} ist ungültig. Hinweis: -1 (alle) oder &gt;= 0.</translation>
    </message>
    <message>
        <source>Parameter &apos;Partition End&apos;: Value {value} is invalid. Hint: -1 (all) or &gt;= 0.</source>
        <translation>Parameter &apos;Partitionsende&apos;: Wert {value} ist ungültig. Hinweis: -1 (alle) oder &gt;= 0.</translation>
    </message>
    <message>
        <source>Parameter: Partition Start ({start}) &gt;= Partition End ({end}). Hint: Start must be less than End.</source>
        <translation>Parameter: Partitionsstart ({start}) &gt;= Partitionsende ({end}). Hinweis: Start muss kleiner als Ende sein.</translation>
    </message>
    <message>
        <source>Parameter &apos;Partition Start/End&apos;: &apos;{start}&apos;/&apos;{end}&apos; are not valid integers.</source>
        <translation>Parameter &apos;Partitionsstart/Partitionsende&apos;: &apos;{start}&apos;/&apos;{end}&apos; sind keine gültigen Ganzzahlen.</translation>
    </message>
</context>

</TS>
```

- [ ] **Step 2: Commit**

```bash
git add i18n/IBTool_de.ts
git commit -m "feat(i18n): add complete German translation file (IBTool_de.ts)"
```

---

## Task 2 — Fix translator path bug in `ibtool.py`

**Files:**
- Modify: `ibtool/ibtool/ibtool.py:114-123`

The translator currently looks for `ibtool/ibtool/i18n/IBTool_de.qm` (wrong).
It should look in `ibtool/i18n/IBTool_de.qm` (plugin root, where the file is placed).

- [ ] **Step 1: Replace the locale path block**

In `ibtool/ibtool/ibtool.py`, find the `__init__` method. Replace:

```python
        self.plugin_dir = os.path.dirname(__file__)
        # Initialize config manager (plugin root is one level above ibtool/ibtool/)
        plugin_root = os.path.dirname(self.plugin_dir)
        self.config_manager = ConfigManager(plugin_root)
        # Initialize locale
        locale = QSettings().value('locale/userLocale')[0:2]
        locale_path = os.path.join(
            self.plugin_dir,
            'i18n',
            f'IBTool_{locale}.qm')
```

With:

```python
        self.plugin_dir = os.path.dirname(__file__)
        # Initialize config manager (plugin root is one level above ibtool/ibtool/)
        plugin_root = os.path.dirname(self.plugin_dir)
        self.config_manager = ConfigManager(plugin_root)
        # Initialize locale — .qm files live in plugin root i18n/, not nested package
        locale = QSettings().value('locale/userLocale')[0:2]
        locale_path = os.path.join(
            plugin_root,
            'i18n',
            f'IBTool_{locale}.qm')
```

- [ ] **Step 2: Run existing tests to confirm nothing broke**

```bash
docker run --rm qgis-plugin-test
```

Expected: all tests pass (path logic change has no effect in test env — no `.qm` file present there).

- [ ] **Step 3: Delete the old i18n files**

```bash
git rm i18n/de.ts i18n/de.qm
```

- [ ] **Step 4: Commit**

```bash
git add ibtool/ibtool/ibtool.py
git commit -m "fix(i18n): correct translator .qm lookup path to plugin root i18n/; remove stale de.ts/de.qm"
```

---

## Task 3 — Add `_tr()` helper and wrap `quick_path_check` in `check.py`

**Files:**
- Modify: `ibtool/helpers/check.py:1-64`

- [ ] **Step 1: Add `_tr()` at the top of `check.py`**

After the existing imports (after `from qgis import processing`), add:

```python
from qgis.PyQt.QtCore import QCoreApplication


def _tr(text: str) -> str:
    """Translate a string using the InputValidator context.

    Returns the original string when no translator is installed (English fallback).
    """
    return QCoreApplication.translate('InputValidator', text)
```

- [ ] **Step 2: Replace `quick_path_check` body**

Replace the three `return False, "..."` lines:

```python
    def quick_path_check(self, path: str, is_dir: bool = False) -> tuple:
        if not path or not path.strip():
            return False, _tr("No path specified")
        if not os.path.exists(path):
            return False, _tr("File or directory does not exist")
        if is_dir and not os.path.isdir(path):
            return False, _tr("Not a directory")
        return True, ""
```

- [ ] **Step 3: Run existing check.py tests to verify messages unchanged (English env)**

```bash
docker run --rm qgis-plugin-test pytest test/ -k "check" -v
```

Expected: all tests pass — `QCoreApplication.translate` returns the source string unchanged when no translator is installed.

- [ ] **Step 4: Commit**

```bash
git add ibtool/helpers/check.py
git commit -m "feat(i18n): add _tr() helper and wrap quick_path_check messages"
```

---

## Task 4 — Wrap layer-loop and geometry check messages in `check.py`

**Files:**
- Modify: `ibtool/helpers/check.py` — `validate_all`, `_check_crs`, `_check_feature_counts`, `_check_geometries`, `_check_validity_processing`

- [ ] **Step 1: Replace `validate_all` layer loop**

Inside `validate_all`, replace the `layer_paths` dict and the loop that follows:

```python
        layer_paths = {
            "Building footprints (HU)": hu_path,
            "Road network (RN)": rn_path,
            "Partitioning (Part)": part_path,
            "Auxiliary layer (Aux)": aux_path,
        }

        valid_layers = {}

        for name, path in layer_paths.items():
            if not path or not path.strip():
                result.add_error(
                    _tr("{layer}: No file path specified.").format(layer=_tr(name))
                )
                continue

            if not os.path.exists(path):
                result.add_error(
                    _tr("{layer}: File does not exist: {path}").format(
                        layer=_tr(name), path=path)
                )
                continue

            layer = QgsVectorLayer(path, name, "ogr")
            if not layer.isValid():
                result.add_error(
                    _tr("{layer}: File cannot be loaded as a valid layer: {path}").format(
                        layer=_tr(name), path=path)
                )
                continue

            valid_layers[name] = layer
            self._check_crs(layer, name, spatial_reference, result)
```

- [ ] **Step 2: Replace `_check_crs`**

```python
    def _check_crs(self, layer, layer_name, expected_crs, result):
        layer_crs = layer.crs()
        if layer_crs.authid() != expected_crs.authid():
            actual = layer_crs.authid() or "undefined/unknown"
            result.add_error(
                _tr("{layer}: CRS mismatch. Expected: {expected}, found: {actual}. "
                    "Hint: Reproject the layer to {expected}.").format(
                    layer=_tr(layer_name), expected=expected_crs.authid(), actual=actual)
            )
```

- [ ] **Step 3: Replace `_check_feature_counts`**

```python
    def _check_feature_counts(self, valid_layers, result):
        min_counts = {
            "Building footprints (HU)": self.MIN_FEATURES_HU,
            "Road network (RN)": self.MIN_FEATURES_RN,
            "Partitioning (Part)": self.MIN_FEATURES_PART,
            "Auxiliary layer (Aux)": self.MIN_FEATURES_AUX,
        }

        for name, min_count in min_counts.items():
            if name not in valid_layers:
                continue
            layer = valid_layers[name]
            count = layer.featureCount()
            if count == 0:
                result.add_error(
                    _tr("{layer}: Layer is empty (0 features). "
                        "Hint: Populate the layer with data.").format(layer=_tr(name))
                )
            elif count < min_count:
                result.add_error(
                    _tr("{layer}: Too few features ({count}), at least {min_count} required. "
                        "Hint: Check the dataset for completeness.").format(
                        layer=_tr(name), count=count, min_count=min_count)
                )
```

- [ ] **Step 4: Replace `_check_geometries` error/warning calls**

Keep the loop logic unchanged; replace only the three `result.add_*` calls at the end:

```python
        if null_count > 0:
            result.add_error(
                _tr("{layer}: {null_count} features with NULL geometry. "
                    "Hint: Remove features without geometry.").format(
                    layer=_tr(layer_name), null_count=null_count)
            )

        if empty_count > 0:
            result.add_error(
                _tr("{layer}: {empty_count} features with empty geometry. "
                    "Hint: Remove features with empty geometry.").format(
                    layer=_tr(layer_name), empty_count=empty_count)
            )

        if invalid_count > 0:
            details = ""
            if invalid_reasons:
                details = " Examples: " + "; ".join(invalid_reasons) + "."
            result.add_warning(
                _tr("{layer}: {invalid_count} invalid geometries (isGeosValid=False)."
                    "{details} Hint: Fix geometries (native:fixgeometries).").format(
                    layer=_tr(layer_name), invalid_count=invalid_count, details=details)
            )
```

- [ ] **Step 5: Replace `_check_validity_processing` result calls**

Replace only the two `result.add_warning` calls:

```python
            result.add_warning(
                _tr("{layer}: 'qgis:checkvalidity' found {invalid_count} invalid features."
                    "{details} Hint: Fix geometries (native:fixgeometries).").format(
                    layer=_tr(layer_name), invalid_count=invalid_count, details=details)
            )
```

and the except:

```python
        except Exception as e:  # pylint: disable=broad-exception-caught
            result.add_warning(
                _tr("{layer}: 'qgis:checkvalidity' could not be executed: {error}").format(
                    layer=_tr(layer_name), error=e)
            )
```

- [ ] **Step 6: Run tests**

```bash
docker run --rm qgis-plugin-test pytest test/ -k "check or valid" -v
```

Expected: all tests pass.

- [ ] **Step 7: Commit**

```bash
git add ibtool/helpers/check.py
git commit -m "feat(i18n): wrap layer loop and geometry check messages in check.py"
```

---

## Task 5 — Wrap layer-specific and ratio check messages in `check.py`

**Files:**
- Modify: `ibtool/helpers/check.py` — `_check_hu_layer`, `_check_rn_layer`, `_check_aux_layer`, `_check_part_layer`, `_check_multipart_lines`, `_check_part_hu_ratio`

- [ ] **Step 1: Replace `_check_hu_layer` error/warning calls**

```python
    def _check_hu_layer(self, layer, result):
        if layer.geometryType() != QgsWkbTypes.PolygonGeometry:
            geom_type = QgsWkbTypes.geometryDisplayString(layer.geometryType())
            result.add_error(
                _tr("Building footprints (HU): Polygon geometry required, but {geom_type} found. "
                    "Hint: Use a layer with polygon geometry.").format(geom_type=geom_type)
            )

        field_names = [f.name() for f in layer.fields()]
        fkt_field = None
        for f in self.HU_REQUIRED_FIELDS:
            if f in field_names:
                fkt_field = f
                break

        if not fkt_field:
            result.add_error(
                _tr("Building footprints (HU): Field 'fkt', 'gfkzshh' or 'funktion' missing. "
                    "Available fields: {fields}. "
                    "Hint: Add a field 'fkt', 'gfkzshh' or 'funktion' containing "
                    "building function codes.").format(fields=', '.join(field_names[:15]))
            )
            return

        null_count = 0
        invalid_format = []

        for feature in layer.getFeatures():
            val = feature[fkt_field]
            if val is None or str(val).strip() in ("", "NULL"):
                null_count += 1
                continue
            if str(val).strip() == "0":
                continue
            if not self.HU_FKT_PATTERN.match(str(val)):
                if len(invalid_format) < 5:
                    invalid_format.append(str(val)[:20])

        if null_count > 0:
            result.add_warning(
                _tr("Building footprints (HU): {null_count} features with "
                    "empty/NULL value in field '{field}'. "
                    "Hint: All features require a building function code.").format(
                    null_count=null_count, field=fkt_field)
            )

        if invalid_format:
            result.add_warning(
                _tr("Building footprints (HU): Values in field '{field}' do not match "
                    "the ATKIS format (NNNNN_NNNN, e.g. 31001_1000). Examples: {examples}. "
                    "Hint: Only the first 10 characters are used for filter matching.").format(
                    field=fkt_field, examples=', '.join(invalid_format))
            )
```

- [ ] **Step 2: Replace `_check_rn_layer` and `_check_aux_layer`**

```python
    def _check_rn_layer(self, layer, result):
        if layer.geometryType() != QgsWkbTypes.LineGeometry:
            geom_type = QgsWkbTypes.geometryDisplayString(layer.geometryType())
            result.add_error(
                _tr("Road network (RN): Line geometry required, but {geom_type} found. "
                    "Hint: Use a layer with line geometry.").format(geom_type=geom_type)
            )

    def _check_aux_layer(self, layer, result):
        if layer.geometryType() != QgsWkbTypes.LineGeometry:
            geom_type = QgsWkbTypes.geometryDisplayString(layer.geometryType())
            result.add_error(
                _tr("Auxiliary layer (Aux): Line geometry required, but {geom_type} found. "
                    "Hint: Use a layer with line geometry.").format(geom_type=geom_type)
            )
```

- [ ] **Step 3: Replace `_check_part_layer`**

```python
    def _check_part_layer(self, layer, result):
        if layer.geometryType() != QgsWkbTypes.PolygonGeometry:
            geom_type = QgsWkbTypes.geometryDisplayString(layer.geometryType())
            result.add_error(
                _tr("Partitioning (Part): Polygon geometry required, but {geom_type} found. "
                    "Hint: Use a layer with polygon geometry.").format(geom_type=geom_type)
            )

        field_names = [f.name() for f in layer.fields()]

        if self.PART_REQUIRED_FIELD not in field_names:
            result.add_error(
                _tr("Partitioning (Part): Field '{field}' missing. "
                    "Available fields: {fields}. "
                    "Hint: Add a text field 'NAME' with partition names "
                    "(e.g. PART_123).").format(
                    field=self.PART_REQUIRED_FIELD,
                    fields=', '.join(field_names[:15]))
            )
            return

        name_idx = layer.fields().indexFromName(self.PART_REQUIRED_FIELD)
        null_count = 0
        non_matching = []

        for feature in layer.getFeatures():
            val = feature[name_idx]
            if val is None or str(val).strip() == "" or str(val) == "NULL":
                null_count += 1
                continue
            name_val = str(val).strip()
            if not self.PART_NAME_REGEX.match(name_val):
                if len(non_matching) < 5:
                    non_matching.append(name_val)

        if null_count > 0:
            result.add_error(
                _tr("Partitioning (Part): {null_count} features with empty/NULL value "
                    "in field 'NAME'. Hint: All partitions require a name in the format "
                    "PART_<number>.").format(null_count=null_count)
            )

        if non_matching:
            result.add_error(
                _tr("Partitioning (Part): NAME values do not match the format "
                    "PART_<number>. Examples: {examples}. Hint: NAME values must exactly "
                    "follow the pattern PART_123 (e.g. PART_36, PART_433).").format(
                    examples=', '.join(non_matching))
            )
```

- [ ] **Step 4: Replace `_check_multipart_lines` result call**

```python
        if multiline_count > 0:
            result.add_error(
                _tr("{layer}: {count} features contain multiple line strings "
                    "(MultiLineString with >1 part). Each feature may only contain one "
                    "line string. Hint: Explode multipart features "
                    "(native:multiparttosingleparts).").format(
                    layer=_tr(layer_name), count=multiline_count)
            )
```

- [ ] **Step 5: Replace `_check_part_hu_ratio` result call**

```python
        if ratio > self.MAX_PART_TO_HU_RATIO:
            result.add_warning(
                _tr("Part:HU ratio = 1:{ratio} (threshold: 1:{threshold}). "
                    "Part has {part_count} features, HU has {hu_count} features. "
                    "Hint: Use a finer partitioning to reduce processing time "
                    "per partition.").format(
                    ratio=f"{ratio:.0f}", threshold=self.MAX_PART_TO_HU_RATIO,
                    part_count=part_count, hu_count=hu_count)
            )
```

- [ ] **Step 6: Run tests**

```bash
docker run --rm qgis-plugin-test pytest test/ -v
```

Expected: all tests pass.

- [ ] **Step 7: Commit**

```bash
git add ibtool/helpers/check.py
git commit -m "feat(i18n): wrap layer-specific and ratio check messages in check.py"
```

---

## Task 6 — Wrap filter, output, and parameter check messages in `check.py`

**Files:**
- Modify: `ibtool/helpers/check.py` — `_read_filter_file`, `_validate_filter_structure`, `_validate_filter_entries`, `_check_output_paths`, `_check_numeric_params`, `_check_spatial_reference_param`, `_check_partition_range_params`

- [ ] **Step 1: Replace `_read_filter_file` error calls**

```python
    def _read_filter_file(self, filter_path, result):
        if not filter_path or not filter_path.strip():
            result.add_error(_tr("Filter file: No file path specified."))
            return None

        if not os.path.exists(filter_path):
            result.add_error(
                _tr("Filter file: File does not exist: {path}").format(path=filter_path)
            )
            return None

        try:
            with open(filter_path, 'r', encoding='utf-8') as f:
                return f.readlines()
        except Exception as e:  # pylint: disable=broad-exception-caught
            result.add_error(
                _tr("Filter file: File cannot be read: {error}. "
                    "Hint: File must be UTF-8 encoded and readable.").format(error=e)
            )
            return None
```

- [ ] **Step 2: Replace `_validate_filter_structure` result calls**

```python
    def _validate_filter_structure(self, parsed, result):
        pos_line = parsed["pos_line"]
        neg_line = parsed["neg_line"]
        orphan_lines = parsed["orphan_lines"]

        if pos_line is None:
            result.add_error(
                _tr("Filter file: Section '{section}' missing. "
                    "Hint: Add the line '{section}' to the file.").format(
                    section=self.FILTER_SECTION_POSITIVE)
            )
        if neg_line is None:
            result.add_error(
                _tr("Filter file: Section '{section}' missing. "
                    "Hint: Add the line '{section}' to the file.").format(
                    section=self.FILTER_SECTION_NEGATIVE)
            )
        if pos_line is not None and neg_line is not None and pos_line > neg_line:
            result.add_error(
                _tr("Filter file: '{pos}' (line {pos_line}) must appear before "
                    "'{neg}' (line {neg_line}).").format(
                    pos=self.FILTER_SECTION_POSITIVE, pos_line=pos_line,
                    neg=self.FILTER_SECTION_NEGATIVE, neg_line=neg_line)
            )
        if orphan_lines:
            samples = [f"line {n}: {t[:30]}" for n, t in orphan_lines[:3]]
            result.add_warning(
                _tr("Filter file: {count} lines appear before the first section header "
                    "and will be ignored. Examples: {examples}.").format(
                    count=len(orphan_lines), examples='; '.join(samples))
            )
```

- [ ] **Step 3: Replace `_validate_filter_entries` result calls**

```python
    def _validate_filter_entries(self, parsed, result):
        pos_line = parsed["pos_line"]
        neg_line = parsed["neg_line"]
        entries_positive = parsed["entries_positive"]
        entries_negative = parsed["entries_negative"]

        if pos_line is not None and len(entries_positive) == 0:
            result.add_error(
                _tr("Filter file: Section '{section}' contains no entries. "
                    "Hint: Add at least one function code.").format(
                    section=self.FILTER_SECTION_POSITIVE)
            )
        if neg_line is not None and len(entries_negative) == 0:
            result.add_error(
                _tr("Filter file: Section '{section}' contains no entries. "
                    "Hint: Add at least one function code.").format(
                    section=self.FILTER_SECTION_NEGATIVE)
            )

        invalid_entries = []
        for line_nr, entry in entries_positive + entries_negative:
            code = entry[:10].strip().rstrip(",")
            if not self.HU_FKT_PATTERN.match(code):
                if len(invalid_entries) < 5:
                    invalid_entries.append(f"line {line_nr}: {entry[:30]}")

        if invalid_entries:
            result.add_warning(
                _tr("Filter file: Entries do not match the ATKIS format (NNNNN_NNNN). "
                    "Examples: {examples}. "
                    "Hint: Only the first 10 characters are used for filter matching.").format(
                    examples='; '.join(invalid_entries))
            )
```

- [ ] **Step 4: Replace `_check_output_paths`**

```python
    def _check_output_paths(self, output_path, workspace_path, result):
        if not output_path or not output_path.strip():
            result.add_error(_tr("Output file: No path specified."))
        else:
            if not output_path.lower().endswith(".gpkg"):
                result.add_error(
                    _tr("Output file: GeoPackage (.gpkg) required, but got: {path}. "
                        "Hint: Choose a .gpkg output file.").format(path=output_path)
                )
            output_dir = os.path.dirname(output_path)
            if not output_dir:
                result.add_error(
                    _tr("Output file: Path must include a directory: {path}. "
                        "Hint: Choose a full output path such as "
                        "C:/data/result.gpkg.").format(path=output_path)
                )

        if not workspace_path or not workspace_path.strip():
            result.add_error(_tr("Workspace directory: No path specified."))
```

- [ ] **Step 5: Replace `_check_numeric_params`**

```python
    def _check_numeric_params(self, params, result):
        numeric_checks = [
            ("min_overlap_blocks", _tr("Min. Overlap Blocks (%)"), float, 0, 100),
            ("global_footprint_density", _tr("Global Footprint Density (%)"), float, 0, 100),
            ("min_area", _tr("Min. Building Area (sqm)"), float, 10, 500),
            ("min_bdg_count", _tr("Min. Building Count"), int, 1, 100),
            ("min_patch_size", _tr("Min. Patch Size (sqm)"), float, 100, 100000),
            ("max_hole_size", _tr("Max. Hole Size (sqm)"), float, 0, 100000),
            ("max_gap_size", _tr("Max. Gap Size (sqm)"), float, 0, 100000),
        ]

        for key, label, num_type, min_val, max_val in numeric_checks:
            raw = params.get(key)
            if raw is None:
                continue

            raw_str = str(raw).strip()
            if not raw_str:
                result.add_error(
                    _tr("Parameter '{label}': No value specified.").format(label=label)
                )
                continue

            try:
                value = num_type(raw_str)
            except (ValueError, TypeError):
                kind = _tr("integer") if num_type == int else _tr("number")
                result.add_error(
                    _tr("Parameter '{label}': '{value}' is not a valid {kind}.").format(
                        label=label, value=raw_str, kind=kind)
                )
                continue

            if min_val is not None and value < min_val:
                result.add_error(
                    _tr("Parameter '{label}': Value {value} is less than the "
                        "minimum ({min_val}).").format(
                        label=label, value=value, min_val=min_val)
                )
            if max_val is not None and value > max_val:
                result.add_error(
                    _tr("Parameter '{label}': Value {value} is greater than the "
                        "maximum ({max_val}).").format(
                        label=label, value=value, max_val=max_val)
                )
```

- [ ] **Step 6: Replace `_check_spatial_reference_param` result calls**

```python
        if not crs.isValid():
            result.add_error(
                _tr("Parameter 'CRS': '{crs}' is not a valid CRS. "
                    "Hint: Use e.g. EPSG:25832.").format(crs=sr_text)
            )
        elif crs.isGeographic():
            result.add_warning(
                _tr("Parameter 'CRS': '{crs}' is a geographic CRS (degrees). "
                    "Hint: A projected CRS (metres) such as EPSG:25832 "
                    "is recommended.").format(crs=sr_text)
            )
```

- [ ] **Step 7: Replace `_check_partition_range_params` result calls**

```python
                if start_val < 0:
                    result.add_error(
                        _tr("Parameter 'Partition Start': Value {value} is invalid. "
                            "Hint: -1 (all) or >= 0.").format(value=start_val)
                    )
                if end_val < 0:
                    result.add_error(
                        _tr("Parameter 'Partition End': Value {value} is invalid. "
                            "Hint: -1 (all) or >= 0.").format(value=end_val)
                    )
                if 0 <= end_val <= start_val:
                    result.add_error(
                        _tr("Parameter: Partition Start ({start}) >= Partition End ({end}). "
                            "Hint: Start must be less than End.").format(
                            start=start_val, end=end_val)
                    )
        except ValueError:
            result.add_error(
                _tr("Parameter 'Partition Start/End': '{start}'/'{end}' are not "
                    "valid integers.").format(start=part_start, end=part_end)
            )
```

- [ ] **Step 8: Run full test suite**

```bash
docker run --rm qgis-plugin-test pytest test/ -v
```

Expected: all tests pass.

- [ ] **Step 9: Commit**

```bash
git add ibtool/helpers/check.py
git commit -m "feat(i18n): wrap filter, output, and parameter check messages in check.py"
```

---

## Task 7 — Wrap strings in `ibtool_dialog.py`

**Files:**
- Modify: `ibtool/ibtool/ibtool_dialog.py`

- [ ] **Step 1: Add `QCoreApplication` import**

At the top of `ibtool_dialog.py`, change the QtCore import line:

```python
from qgis.PyQt.QtCore import Qt
```

to:

```python
from qgis.PyQt.QtCore import Qt, QCoreApplication
```

- [ ] **Step 2: Replace module-level label constants with lazy functions**

Replace:

```python
_STEP_LABELS = [
    "① Input",
    "② Parameters",
    "③ Validation",
    "④ Processing",
]
_STEP_SHORT = ["Input", "Parameters", "Validation", "Processing"]
```

With:

```python
def _step_labels():
    tr = lambda s: QCoreApplication.translate('IBToolDialog', s)
    return [
        tr("① Input"),
        tr("② Parameters"),
        tr("③ Validation"),
        tr("④ Processing"),
    ]


def _step_short():
    tr = lambda s: QCoreApplication.translate('IBToolDialog', s)
    return [tr("Input"), tr("Parameters"), tr("Validation"), tr("Processing")]
```

- [ ] **Step 3: Update `set_step` to call the functions**

Replace the two references inside `set_step`:

```python
    def set_step(self, index: int) -> None:
        self.stackedWidget.setCurrentIndex(index)

        labels = _step_labels()
        short = _step_short()
        for i in range(4):
            btn = getattr(self, f'stepBtn{i}')
            if i == index:
                btn.setText(labels[i])
                btn.setStyleSheet(
                    "QToolButton { font-weight: bold; color: #1565C0; "
                    "text-decoration: underline; }"
                )
            elif i < index:
                btn.setText(f"✓ {short[i]}")
                btn.setStyleSheet("QToolButton { color: #757575; }")
            else:
                btn.setText(labels[i])
                btn.setStyleSheet("QToolButton { color: inherit; font-weight: normal; "
                                  "text-decoration: none; }")
```

- [ ] **Step 4: Wrap validation checklist string**

In `populate_validation_checklist`, replace:

```python
            item = QListWidgetItem("✅  All checks passed")
```

With:

```python
            item = QListWidgetItem(
                QCoreApplication.translate('IBToolDialog', "✅  All checks passed")
            )
```

- [ ] **Step 5: Wrap `FilterPreviewDialog` strings**

In `FilterPreviewDialog.__init__`, replace:

```python
        self.setWindowTitle("Filter entries")
```

With:

```python
        self.setWindowTitle(QCoreApplication.translate('IBToolDialog', "Filter entries"))
```

In `_build_ui`, replace:

```python
        pos_label = QLabel("<b>Positive filter</b>")
```

With:

```python
        pos_label = QLabel(
            "<b>" + QCoreApplication.translate('IBToolDialog', "Positive filter") + "</b>"
        )
```

And:

```python
        neg_label = QLabel("<b>Negative filter</b>")
```

With:

```python
        neg_label = QLabel(
            "<b>" + QCoreApplication.translate('IBToolDialog', "Negative filter") + "</b>"
        )
```

- [ ] **Step 6: Run tests**

```bash
docker run --rm qgis-plugin-test pytest test/ -v
```

Expected: all tests pass.

- [ ] **Step 7: Commit**

```bash
git add ibtool/ibtool/ibtool_dialog.py
git commit -m "feat(i18n): wrap dialog strings and convert step labels to lazy functions"
```

---

## Task 8 — Wrap strings in `ibtool.py`

**Files:**
- Modify: `ibtool/ibtool/ibtool.py`

All strings in this task use the existing `self.tr()` method, which wraps `QCoreApplication.translate('IBTool', ...)`.

- [ ] **Step 1: Wrap file-dialog titles and filters**

Replace each dialog call. File select dialogs (apply same pattern to all eight):

```python
    def select_hu_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self.dlg,
            self.tr("Select building footprints file"),
            "",
            self.tr("Vector files (*.shp *.gpkg);;Shapefiles (*.shp);;"
                    "GeoPackage (*.gpkg);;All Files (*)")
        )
        if file_path:
            self.dlg.HuPath.setText(file_path)

    def select_rn_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self.dlg,
            self.tr("Select road network file"),
            "",
            self.tr("Vector files (*.shp *.gpkg);;Shapefiles (*.shp);;"
                    "GeoPackage (*.gpkg);;All Files (*)")
        )
        if file_path:
            self.dlg.RnPath.setText(file_path)

    def select_part_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self.dlg,
            self.tr("Select partitions file"),
            "",
            self.tr("Vector files (*.shp *.gpkg);;Shapefiles (*.shp);;"
                    "GeoPackage (*.gpkg);;All Files (*)")
        )
        if file_path:
            self.dlg.PartPath.setText(file_path)

    def select_aux_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self.dlg,
            self.tr("Select auxiliary data file"),
            "",
            self.tr("Vector files (*.shp *.gpkg);;Shapefiles (*.shp);;"
                    "GeoPackage (*.gpkg);;All Files (*)")
        )
        if file_path:
            self.dlg.AuxPath.setText(file_path)

    def select_output_file(self):
        file_path, _ = QFileDialog.getSaveFileName(
            self.dlg,
            self.tr("Select output file"),
            "",
            self.tr("GeoPackage (*.gpkg);;All Files (*)")
        )
        if file_path:
            self.dlg.OutputPath.setText(file_path)

    def select_workspace_file(self):
        folder_path = QFileDialog.getExistingDirectory(
            self.dlg,
            self.tr("Select workspace folder"),
            ""
        )
        if folder_path:
            self.dlg.WorkspacePath.setText(folder_path)

    def select_filter_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self.dlg,
            self.tr("Select filter config file"),
            "",
            self.tr("Text files (*.txt);;All Files (*)")
        )
        if file_path:
            self.dlg.FilterPath.setText(file_path)
            self.load_filter_file(file_path)

    def select_log_dir(self):
        folder_path = QFileDialog.getExistingDirectory(
            self.dlg,
            self.tr("Select log directory"),
            ""
        )
        if folder_path:
            self.dlg.LogDirPath.setText(folder_path)
```

- [ ] **Step 2: Wrap `cancel_processing` message**

```python
    def cancel_processing(self):
        self.update_messages(self.tr("Processing cannot be cancelled mid-run."))
```

- [ ] **Step 3: Wrap result action messages**

```python
    def _load_result_to_qgis(self):
        if not self._last_output_path or not os.path.exists(self._last_output_path):
            msg(self.tr("Output file not found."))
            return
        layer = QgsVectorLayer(
            self._last_output_path, self.tr("IB-Tool result"), "ogr")
        if layer.isValid():
            QgsProject.instance().addMapLayer(layer)
        else:
            msg(f"Could not load result layer: {self._last_output_path}")

    def _open_output_dir(self):
        folder = self._last_output_folder or os.path.dirname(self._last_output_path)
        if folder and os.path.isdir(folder):
            _open_directory(folder)
        else:
            msg(self.tr("Output directory not found."))

    def _export_log(self):
        text = self.dlg.MessageBox.toPlainText()
        if not text:
            msg(self.tr("No log content to export."))
            return
        file_path, _ = QFileDialog.getSaveFileName(
            self.dlg,
            self.tr("Export log"),
            self._last_output_folder or "",
            self.tr("Text files (*.txt);;All Files (*)")
        )
        if file_path:
            try:
                with open(file_path, 'w', encoding='utf-8') as f:
                    f.write(text)
                logger.log(f"Log exported to: {file_path}", level="INFO")
            except OSError as e:
                msg(f"Could not export log: {e}")
```

- [ ] **Step 4: Wrap `_delete_config` QMessageBox**

Replace the existing `QMessageBox.question` call:

```python
        reply = QMessageBox.question(
            self.dlg,
            self.tr("Reset Configuration"),
            self.tr("Delete CONFIG.ini and reset all fields to defaults?\n"
                    "This action cannot be undone."),
            QMessageBox.Yes | QMessageBox.No,
            QMessageBox.No,
        )
```

Also replace the three German log messages in this method:

```python
        # was: logger.log(f"Fehler beim Löschen der CONFIG.ini: {e}", ...)
        logger.log(f"Error deleting CONFIG.ini: {e}", level="CRITICAL")
        # ...
        # was: logger.log("Konfiguration zurückgesetzt — CONFIG.ini gelöscht.", ...)
        logger.log(self.tr("Configuration reset — CONFIG.ini deleted."), level="INFO")
```

- [ ] **Step 5: Wrap `_apply_config_to_ui` log line**

Replace:

```python
        logger.log("Konfiguration aus CONFIG.ini geladen.", level="INFO")
```

With:

```python
        logger.log(self.tr("Configuration loaded from CONFIG.ini."), level="INFO")
```

- [ ] **Step 6: Wrap `_save_config_from_ui` log line**

Replace:

```python
        logger.log("Konfiguration in CONFIG.ini gespeichert.", level="INFO")
```

With:

```python
        logger.log(self.tr("Configuration saved to CONFIG.ini."), level="INFO")
```

- [ ] **Step 7: Wrap `start_processing` status strings and phase names**

```python
        self.dlg.phaseLabel.setText(self.tr("Starting processing..."))
```

Replace the validation-aborted log line:

```python
        # was: logger.log("Verarbeitung abgebrochen wegen Validierungsfehlern.", ...)
        logger.log(self.tr("Processing aborted due to validation errors."), level="CRITICAL")
```

Replace the six `_update_phase` phase-name strings:

```python
        self._update_phase(1, 6, self.tr("Load Data"), 0)
        # ...
        self._update_phase(2, 6, self.tr("Calculate Blocks"), 10)
        # ...
        self._update_phase(3, 6, self.tr("Apply Filter"), 20)
        # ...
        self._update_phase(4, 6, self.tr("Calculate MST"), 40)
        # ...
        self._update_phase(5, 6, self.tr("Clustering"), 60)
        # ...
        self._update_phase(6, 6, self.tr("Erode Empty Areas"), 90)
        # (Save Output phase)
        self._update_phase(6, 6, self.tr("Save Output"), 95)
```

Replace the final status line:

```python
        self.dlg.phaseLabel.setText(self.tr("Processing complete"))
```

- [ ] **Step 8: Run full test suite**

```bash
docker run --rm qgis-plugin-test pytest test/ -v
```

Expected: all tests pass.

- [ ] **Step 9: Commit**

```bash
git add ibtool/ibtool/ibtool.py
git commit -m "feat(i18n): wrap all user-facing strings in ibtool.py with self.tr()"
```

---

## Task 9 — Update `docs/contributing.md`

**Files:**
- Modify: `docs/contributing.md`

- [ ] **Step 1: Append "Updating translations" section**

At the end of `docs/contributing.md`, add:

```markdown
---

## Updating Translations

The German translation file is `i18n/IBTool_de.ts`. It must be kept in sync whenever
user-facing strings change in `ibtool/ibtool/ibtool.py`, `ibtool/ibtool/ibtool_dialog.py`,
`ibtool/helpers/check.py`, or `ibtool/ibtool/ibtool_dialog_base.ui`.

### When you change a string

1. Find the matching `<source>` element in `i18n/IBTool_de.ts`.
2. Update (or add) its `<translation>` element with the German text.
3. Recompile (see below).

### When you add a new string

1. Wrap it in `self.tr()` / `QCoreApplication.translate('<ContextName>', ...)` / `_tr()` in the source.
2. Add a new `<message>` block to the correct `<context>` in `IBTool_de.ts`:

```xml
<message>
    <source>Your new English string</source>
    <translation>Ihre neue deutsche Zeichenkette</translation>
</message>
```

Context names:  
- `IBToolDialogBase` — strings from `ibtool_dialog_base.ui`  
- `IBTool` — strings in `ibtool.py` (via `self.tr()`)  
- `IBToolDialog` — strings in `ibtool_dialog.py`  
- `InputValidator` — strings in `helpers/check.py` (via `_tr()`)

### Compile after editing

```bash
# Linux / macOS
lrelease i18n/IBTool_de.ts -qm i18n/IBTool_de.qm

# Windows (QGIS bundled lrelease)
"C:\Program Files\QGIS 3.x\bin\lrelease.exe" i18n/IBTool_de.ts -qm i18n/IBTool_de.qm
```

Reload the plugin in QGIS to pick up the new `.qm`.

### Extract new strings automatically (optional)

```bash
pylupdate5 ibtool/ibtool/ibtool.py ibtool/ibtool/ibtool_dialog.py \
  ibtool/helpers/check.py ibtool/ibtool/ibtool_dialog_base.ui \
  -ts i18n/IBTool_de.ts
```

This adds new `<source>` entries without overwriting existing `<translation>` values.
Fill in the `<translation>` tags manually, then recompile.

### Adding a new language

Copy `i18n/IBTool_de.ts` to `i18n/IBTool_<locale>.ts` (e.g. `IBTool_fr.ts`),
fill in the `<translation>` elements, and compile with `lrelease`.
No Python changes needed — QGIS picks up the locale automatically.
```

- [ ] **Step 2: Commit**

```bash
git add docs/contributing.md
git commit -m "docs: add translation maintenance instructions to contributing.md"
```

---

## Task 10 — Compile `.qm` and verify

- [ ] **Step 1: Compile the translation file**

Run from the plugin root:

```bat
"C:\Program Files\QGIS 3.x\bin\lrelease.exe" i18n/IBTool_de.ts -qm i18n/IBTool_de.qm
```

Expected output:
```
Updating 'i18n/IBTool_de.qm'...
    Generated 1 translation(s) (X finished and 0 unfinished)
```

- [ ] **Step 2: Stage and commit the compiled file**

```bash
git add i18n/IBTool_de.qm
git commit -m "feat(i18n): add compiled German translation binary (IBTool_de.qm)"
```

- [ ] **Step 3: Verify in QGIS**

1. Set QGIS language to German: **Settings → Options → General → User interface translation → Deutsch**. Restart QGIS.
2. Reload the IBTool plugin (Plugin Manager → IBTool → Reload).
3. Open IBTool. Verify:
   - Step buttons show "① Eingabe", "② Parameter", "③ Validierung", "④ Verarbeitung"
   - Group boxes show "Eingabedaten", "Siedlungsanalyse", "Validierungsliste", "Fehlersuche"
   - Footer buttons show "Prüfen", "Weiter →", "← Zurück", "Konfiguration speichern"
   - File browser title shows "Gebäudegrundrisse auswählen" when clicking the HU browse button
4. Switch back to English (or any other locale) and verify the original English text is shown.

- [ ] **Step 4: Run full test suite one final time**

```bash
docker run --rm qgis-plugin-test pytest test/ -v
```

Expected: all tests pass.
